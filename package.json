{
  "name": "wp-theme-studio",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Authorized studio to generate WordPress themes, pages, CF7 forms, and images",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:80": "concurrently -n api,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w client -- --host 0.0.0.0 --port 80\"",
    "start": "npm run start -w server",
    "build": "npm run build -w client",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
